function nameFunction(firstName, lastName) {
    const name = {
        firstName,
        lastName
    };

    return name;
};

// console.log(nameFunction("Francesco", "Delmonaco"));

export {
    nameFunction
};